const fs=require('fs');const f='components/cinematic/CinematicPortfolio.tsx';let s=fs.readFileSync(f,'utf8');s=s.replace('done=true;timeline?.kill();','done=true;timeline?.kill();\n      gsap.set(\'.hero-portrait\', { clearProps: \'filter\' });');s=s.replace('aria-label="Opening: every journey starts with a decision">','aria-label="Opening: every journey starts with a decision" onKeyDown={event=>{if(event.key===\'Tab\'){event.preventDefault();skipIntro.current?.focus();}if(event.key===\'Escape\')finishIntro.current();}}>');fs.writeFileSync(f,s);

const {chromium}=require('C:/Users/Usman Farooqi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({viewport:{width:1440,height:900}});await p.goto('http://127.0.0.1:3000/',{waitUntil:'domcontentloaded'});await p.getByRole('button',{name:'Skip introduction'}).waitFor();await p.waitForFunction(()=>document.activeElement?.classList.contains('skip-intro'));await p.keyboard.press('Tab');assert.equal(await p.getByRole('button',{name:'Skip introduction'}).evaluate(el=>el===document.activeElement),true);await p.keyboard.press('Escape');await p.locator('.door-intro').waitFor({state:'hidden'});assert.equal(await p.locator('.hero-portrait').evaluate(el=>getComputedStyle(el).filter),'none');assert.equal(await p.locator('main').evaluate(el=>el.inert),false);await p.reload({waitUntil:'networkidle'});await p.locator('.door-intro').waitFor({state:'hidden'});console.log('Intro skip, focus loop, Escape, cleanup, repeat-visit checks passed.');await p.emulateMedia({reducedMotion:'reduce'});for(const viewport of [{width:320,height:640},{width:390,height:844},{width:768,height:1024},{width:1024,height:768},{width:1440,height:900},{width:1920,height:1080}]){await p.setViewportSize(viewport);await p.reload({waitUntil:'networkidle'});await p.evaluate(()=>document.fonts.ready);console.log('Final layout',await p.evaluate(()=>({width:innerWidth,height:innerHeight,scroll:document.documentElement.scrollWidth,clipped:[...document.querySelectorAll('main h1,main h2,main h3,main .scene-body')].filter(el=>{const r=document.createRange();r.selectNodeContents(el);const scene=el.closest('section').getBoundingClientRect();return [...r.getClientRects()].some(x=>x.width&&(x.left< -2||x.right>innerWidth+2||x.bottom>scene.bottom+3))}).map(el=>el.textContent.slice(0,50))})));}await b.close()})().catch(e=>{console.error(e);process.exit(1)});



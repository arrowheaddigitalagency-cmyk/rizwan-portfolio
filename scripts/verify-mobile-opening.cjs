const {chromium}=require('C:/Users/Usman Farooqi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({viewport:{width:390,height:844}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:3000/#hero',{waitUntil:'domcontentloaded'});await p.waitForFunction(()=>document.querySelector('main')?.inert===true);await p.screenshot({path:'review-cinematic/mobile-opening.jpg',quality:70});await p.locator('.door-intro').waitFor({state:'hidden',timeout:12000});assert.equal(await p.locator('.pin-spacer').count(),0);assert.equal(await p.locator('main').evaluate(el=>el.inert),false);assert.equal(await p.locator('.hero-editorial').evaluate(el=>getComputedStyle(el).opacity),'1');assert.equal(await p.locator('#cars-compound .scene-body').evaluate(el=>getComputedStyle(el).fontSize),'16px');await p.getByRole('button',{name:'Menu +',exact:true}).click();await p.keyboard.press('Escape');assert.equal(await p.getByRole('button',{name:'Menu +',exact:true}).evaluate(el=>el===document.activeElement),true);assert.deepEqual(errors,[]);console.log('PASS: mobile #hero opening, automatic completion, visible hero, 16px body, zero pins, Escape focus, no JS errors.');await b.close()})().catch(e=>{console.error(e);process.exit(1)});

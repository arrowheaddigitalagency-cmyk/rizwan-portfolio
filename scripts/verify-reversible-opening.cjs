const {chromium}=require('C:/Users/Usman Farooqi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});for(const [w,h] of [[390,844],[1440,900]]){const p=await b.newPage({viewport:{width:w,height:h}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:3000/',{waitUntil:'networkidle'});await p.waitForSelector('.has-door-motion');await p.evaluate(()=>window.scrollTo(0,0));await p.waitForTimeout(900);assert.equal(await p.locator('#hero').evaluate(e=>e.inert),true);await p.evaluate(h=>window.scrollTo(0,h*.5),h);await p.waitForTimeout(1000);await p.screenshot({path:`review-cinematic/reveal-${w}-middle.jpg`,quality:60});const middle=await p.locator('.door-left').evaluate(e=>new DOMMatrix(getComputedStyle(e).transform).m41);assert.ok(middle < -20);await p.evaluate(h=>window.scrollTo(0,h),h);await p.waitForTimeout(1000);assert.equal(await p.locator('#hero').evaluate(e=>e.inert),false);assert.equal(await p.locator('#opening').evaluate(e=>getComputedStyle(e).visibility),'hidden');await p.evaluate(()=>window.scrollTo(0,0));await p.waitForTimeout(1100);assert.equal(await p.locator('#opening').evaluate(e=>getComputedStyle(e).visibility),'visible');assert.ok(Math.abs(await p.locator('.door-left').evaluate(e=>new DOMMatrix(getComputedStyle(e).transform).m41))<1);await p.getByRole('link',{name:'Begin the journey'}).click();await p.waitForTimeout(2200);assert.equal(await p.locator('#opening').evaluate(e=>getComputedStyle(e).visibility),'hidden');assert.equal(await p.locator('#hero').evaluate(e=>e.inert),false);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);console.log(w,'PASS: partial reveal, full hero, reverse closure, Begin link, overflow, errors');await p.emulateMedia({reducedMotion:'reduce'});await p.waitForTimeout(300);assert.equal(await p.locator('.has-door-motion').count(),0);assert.equal(await p.locator('#hero').evaluate(e=>e.inert),false);console.log(w,'PASS reduced-motion natural sections');await p.close()}await b.close()})().catch(e=>{console.error(e);process.exit(1)});
